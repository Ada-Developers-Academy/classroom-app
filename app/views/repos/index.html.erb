<table>
  <thead>
    <tr>
      <th>Cohort Num</th>
      <th>Repo URL</th>
    </tr>
    <% @repos.each do |r| %>
      <tr>
        <td><%= r.cohort_num %></td>
        <td><%= link_to r.repo_url, r.repo_url %></td>
      </tr>
    <% end %>
  </thead>
</table>
